@* @model ECommerceApp.Models.Product

@{
    ViewData["Title"] = "Product Details";
}

<h1>Product Details</h1>

<div>
    <h4>Product ID: @Model.ProductId</h4>
    <h4>Name: @Model.Name</h4>
    <h4>Description: @Model.Description</h4>
    <h4>Price: @Model.Price</h4>
    <h4>Category: @Model.Category</h4>
</div>

<a asp-controller="ProductMvc" asp-action="Delete" asp-route-id="@Model.ProductId" class="btn btn-primary">Delete Product</a>
<a asp-controller="ProductMvc" asp-action="Index" class="btn btn-secondary">Back to List</a> *@
@model ECommerceApp.Models.Product

@{
    ViewData["Title"] = "Delete Product";
}

<h1>Delete Product</h1>

<div>
    <h4>Are you sure you want to delete this product?</h4>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>

    <form asp-action="Delete" asp-route-id="@Model.ProductId" method="post">
        @Html.AntiForgeryToken()
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>
